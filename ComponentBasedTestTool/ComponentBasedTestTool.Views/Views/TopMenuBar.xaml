<UserControl x:Class="ComponentBasedTestTool.Views.Views.TopMenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ComponentBasedTestTool.Views.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <DockPanel>
    <Menu DockPanel.Dock="Top">
      <MenuItem Header="_File">
        <MenuItem Header="_Open" Command="{Binding RestoreWorkspaceCommand}"/>
        <MenuItem Header="_Close"/>
        <MenuItem Header="_Save" Command="{Binding SaveWorkspaceCommand}"/>
      </MenuItem>
    </Menu>
    <StackPanel></StackPanel>
  </DockPanel>
</UserControl>
